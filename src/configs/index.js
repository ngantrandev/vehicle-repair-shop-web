const routes = {
    login: '/login',
    register: '/register',
    home: '/home',
    resetPassword: '/reset-password',
    createService: '/service/new',
};

const configs = {
    routes,
};

export default configs;
